# public/icons

This project's icon system is implemented as inline SVGs in
`src/components/astro/Icon.astro` (no icon font or sprite sheet needed).

This folder is kept for any standalone icon/app-icon assets you want to add
later (e.g. PWA icons, favicons at multiple sizes, social sharing icons).
